name: Keep Render Warm

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping warmup endpoint
        run: curl -s --fail https://mini-games-backend-h7ab.onrender.com/warmup > /dev/null
